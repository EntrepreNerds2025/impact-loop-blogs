import post from './post';
import author from './author';
import category from './category';
import portableText from './portableText';

export const schemaTypes = [post, author, category, portableText];
