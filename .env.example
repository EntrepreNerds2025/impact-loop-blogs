# Brand selection (set per Vercel deployment)
# BRAND=impact-loop

# Sanity CMS
NEXT_PUBLIC_SANITY_PROJECT_ID=ngkvlovw
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=           # Create at sanity.io/manage → API → Tokens → Add token (Editor role)
